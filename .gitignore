.DS_Store
__pycache__/
*.pyc
*.pyo
.venv/
node_modules/
.env
.env.local
.vscode/
.idea/
*.swp
*.swo
*~
.cf-pages-output/
# wrangler 本機 cache（跑 wrangler 後生成；未忽略會讓 update-fixtures.py step 0 dirty-tree guard 誤判 abort）
.wrangler/

# OpenAI raw responses — keep locally for audit, don't commit (含 search reasoning trace)
fixtures/*.raw.json

# 射手榜缺中文譯名清單（build-standings 每跑重生的暫態 artifact，不進 commit；
# 否則下次 update-fixtures.py step 0「working tree clean」guard 會誤判 dirty 而 abort）
scripts/player-zh-missing.txt

# 製作 backstage（含模型名/工作稿/workflow）— 不進公開 repo
editorial/

# 本機 Claude session 狀態（worktrees / local settings）
.claude/
